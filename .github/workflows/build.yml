on:
  pull_request:
    branches:
      - main
      - rel-1877
      - sta-1877
  push:
    branches:
      - main
      - rel-1877
      - sta-1877
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
jobs:
  build:
    uses: gardenlinux/package-build/.github/workflows/build.yml@bd7d5cc46ce721a076727db145cbee3a2e5257ea # main
    with:
      publish_release: ${{ github.event_name != 'pull_request' }}
      release: false
