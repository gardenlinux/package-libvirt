on:
  pull_request:
    branches:
      - main
      - rel-1877
      - sta-1877
  push:
    branches:
      - main
      - rel-1877
      - sta-1877
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
jobs:
  build:
    uses: gardenlinux/package-build/.github/workflows/build.yml@0378caf3003ef453620b2e4312199e11e67955a7 # main
    with:
      publish_release: ${{ github.event_name != 'pull_request' }}
      release: false
